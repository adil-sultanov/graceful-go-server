# graceful-go-server

A minimal HTTP server built with Go standard library demonstrating:

graceful shutdown with SIGTERM

request cancellation via context

clean lifecycle management

Built to understand real backend service behavior under termination.
